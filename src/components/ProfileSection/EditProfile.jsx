import React from 'react'

const EditProfile = ({Profile}) => {
  return (
    <div className={`${Profile?'hidden':'block'} flex-2`}>
        EditProfile
        </div>
  )
}

export default EditProfile